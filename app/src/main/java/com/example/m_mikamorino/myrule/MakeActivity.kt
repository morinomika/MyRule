package com.example.m_mikamorino.myrule

import android.content.Intent
import android.support.v7.app.AppCompatActivity
import android.os.Bundle
import android.view.View
import kotlinx.android.synthetic.main.activity_make.*

class MakeActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_make)
        //create data value
        //save value




//        return_button.setOnClickListener(object: View.OnClickListener {
//            override fun onClick(view: View): Unit {
//                val rule = editText.text.toString()
//                print(rule);
////                val data = RuleName(rule)
//                // Handler code here.
//                val intent = Intent(this@MakeActivity, MainActivity::class.java)
//                intent.putExtra("RuleName", "data")
//                startActivity(intent)

//
//            }
//        })
    }
}
